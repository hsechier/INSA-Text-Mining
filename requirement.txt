conda create -n text_mining python=3.5.0 numpy pip matplotlib=2.2.3 ipykernel
activate text_mining
pip install pandas nltk sklearn gensim tqdm ipywidgets langdetect seaborn plotly pyldavis

python -m ipykernel install --user --name text_mining

jupyter nbextension enable --py --sys-prefix widgetsnbextension 
conda install matplotlib

conda env remove -n text_mining


# Env creation via conda
# Env activation
# Package Installation
# Enable jupyter kernel
# Enable tqdm extension
# Remove the environnement